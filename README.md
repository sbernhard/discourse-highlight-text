# discourse-highlight-text theme component

Uses https://markjs.io/ to highlight text in discourse.
Supports mark() and markRegExp() methods.

Uses 'color: $quaternary' (discourse -> customize -> colors) to highlight the text. 
